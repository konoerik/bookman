# Context
<!-- Updated by /wrap at session end. Edit manually if needed. Keep it under 8 lines. -->

**Current focus:** Closing out v1.0 (FEATURES *v1.0 scope*). Nothing is half-finished — the spec/code binding and the whole TUI-unblocking slice landed and are green at 402 tests, ruff/mypy clean. What remains is a list, not a thread: A10, D9, five ❓ rows that pin format-grouping (A4, A8, F5, F13, F16), and CLI commands for K1–K3 (N7 is 🔶 because of that gap).
**Last session:** Settled the spec review by keeping both documents and inverting their relationship — `docs/IDENTIFICATION.md` is now the source of truth, `docs/FEATURES.md` Part I the conformance report against it (`Step` column on 86 rows), enforced by `tests/test_spec_conformance.py` and an executable 20-row decision table (ADR-16); then `Book.id` in metadata.json v3 (ADR-17) and curation — `mark_reviewed`/`edit`/`reidentify` (ADR-18). Also: MOBI deprioritized below v1.0 (ADR-2 Amendment), v1.0 line drawn, format boundary written down. The conformance audit found no unregistered divergence.
**Blocking:** Three decisions, none blocking the list above — **OQ3/C7** (is an edition a different book), **OQ1/F14** (two files of one format kind), and whether volumes should be *related* (I6) rather than merely kept apart. Carried over: the first CI run's Windows leg still hasn't been looked at.
**Next action:** **A10** — the one open spec divergence: IDENT-1 says try each ISBN in turn, `resolve.identify` tries only `isbns[0]` (marked in code with a `DIVERGENCE` comment). Then D9, then the five ❓ tests, then the CLI.
<!-- wrapped: 2026-09-18 -->
