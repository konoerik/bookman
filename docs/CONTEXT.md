# Context
<!-- Updated by /wrap at session end. Edit manually if needed. Keep it under 8 lines. -->

**Current focus:** Releasing v1.0, now with a real bundle behind it. Uncommitted on `main`: ADR-26/27/28 + the four fixes from the first real import (37 No Starch/O'Reilly titles — see `docs/FIELD-NOTES.md`), 485 tests, ruff check/format and mypy clean. Two earlier commits (`f5a0be1`, `517350f`) still unpushed.
**Last session (2026-09-20):** Imported the real bundle in batches into a scratch library. Found and fixed: PDF ISBN scan window 5 → 10 pages (FN-1); placeholder `/Title` "untitled" now falls to the stem per IDENT-4 (FN-3); GROUP-1 joins on any of a file's ISBNs, and a follow-up identified through a *different* ISBN than it joined on no longer renames the book (ADR-26, ADR-27, FN-4 — Rust 3rd ed. was being relabelled 2nd); EPUB embedded cover as fallback (ADR-28, FN-5). Final run: 37 books, 37 EPUB+PDF pairs, 37 covers, 0 failures. MOBI stem-adoption parked (FN-8): stem alone judged too weak. OQ4 opened (book remembers one ISBN → PDF-first order still splits).
**Blocking:** Nothing. The user has many more Humble Bundle books to run; FIELD-NOTES is where their shapes accumulate before "proper fixes".
**Next action:** Commit this work (user's call). Then the remaining pre-release items in the user's order: GitHub-readiness beyond the basics (tag-triggered wheel build to a GitHub Release, classifier → Production/Stable, version 1.0.0), a local `uv build` wheel for the TUI to consume; `docs/API.md` deferred until those are done; then `/prep`, `/release`.
<!-- wrapped: 2026-09-20 -->
