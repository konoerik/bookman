# Context
<!-- Updated by /wrap at session end. Edit manually if needed. Keep it under 8 lines. -->

**Current focus:** Closing out v1.0 (FEATURES *v1.0 scope*). Green at 421 tests, ruff/mypy clean. Two items left on Active: the CLI commands for K1–K3 (N7 is 🔶 until they exist) and F14, which is blocked on OQ1.
**Last session:** Built the CLI commands for K1–K3 (`review`/`edit`/`reidentify`, N7 → ✅, ADR-20), then decided OQ1/F14 as ADR-21 — a same-kind file is refused with the evidence, never overwritten; spec_version 2. Before that: Closed A10 (every ISBN tried in turn), D9 (generational suffixes) and pinned the five ❓ rows (A4, A8, F5, F13, F16 — the code was right); found live that Open Library's `/api/books` 404s on every bibkey and migrated `lookup_by_isbn` to `search.json?isbn=` (+ a `bookman/<version>` User-Agent); found live and fixed a spec hole as ADR-19 (an asserted ISBN is rejected when the title contradicts and no author vouches — spec row 21). Surveyed OL's response fields and noted the usable signals in PLAN Backlog.
**Blocking:** Four decisions, none blocking the CLI — **OQ3/C7** (editions), **OQ1/F14** (two files of one format kind), volumes *related* vs merely apart (I6), and the new one: **file's author stands on a corroborated match** (OL returns work-level authors, so an audiobook narrator lands on the book — seen live). Carried over: CI's Windows leg still unlooked-at.
**Next action:** **CLI commands for K1–K3** (`mark-reviewed`/`edit`/`reidentify`, restoring N7 parity) — the last unblocked v1.0 item. Then the decisions above.
<!-- wrapped: 2026-09-18 -->
